import styled from 'styled-components'

export const Outer = styled.div(() => ({
  background: 'white',
  borderRadius: '5px',
  boxShadow: '4px 1px 8px -1px rgba(46,46,46,0.36)',
  padding: '10px',
  margin: '15px'
}))
